[tool.poetry]
name = "poetry-demo-domroon"
version = "0.1.2"
description = "Test"
authors = ["Dominik Haeusser <haeusser-dominik@web.de>"]

[tool.poetry.dependencies]
python = "^3.10"
pendulum = "^2.1.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
