#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['bluesky_hwproxy', 'bluesky_hwproxy.comms']

package_data = \
{'': ['*']}

install_requires = \
['rich', 'click', 'bluesky-queueserver', 'pyzmq']

entry_points = \
{'console_scripts': ['hwproxy = bluesky_hwproxy.hwproxy_cli:main',
                     'start-hwproxy-server = '
                     'bluesky_hwproxy.hwproxy_server:main']}

setup(name='bluesky-hwproxy',
      version='0.0.1',
      description='Proxy for reading bluesky hardware over ZMQ interface.',
      author=None,
      author_email='Kyle Sunden <contact@ksunden.space>',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
