<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xml:lang="${locale_name}" lang="${locale_name}">
  <head>
    <meta charset="utf-8"/>
    <title>${subject}</title>
    <style type="text/css">  
      .cioEmail {
          font-family: Arial, Helvetica, sans-serif;
          font-size: 13pt;
          color: #4f4f4f;
      }
    </style>
  </head>
  <body>
    <p class="cioEmail">
      ${_('Hello ${n},', {'n': first_name or last_name})}
    </p>
    <div class="cioEmail">
      <div>
        ${_('We invite you to join us on ${n}:', {'n': site_title})}
        <a href="${url}">${url}</a>.
      </div>
      <div>
        ${_('Your identifier is:')} <strong>${login}</strong>
      </div>
    </div>
    <p class="cioEmail"><em>${_('${n} Team', {'n': site_title})}</em></p>
  </body>
</html>
