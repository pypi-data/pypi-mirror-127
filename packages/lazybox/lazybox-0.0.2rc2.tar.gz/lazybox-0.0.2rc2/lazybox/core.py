# AUTOGENERATED! DO NOT EDIT! File to edit: core.ipynb (unless otherwise specified).

__all__ = ['DataBox']

# Cell
class DataBox:

    def __init__(self, block, getter, *tfms):

        self.block = tuplify(block)
        self.getter = getter
        self.tfms = tfms


    def __len__(self) -> int:
        return len(tuplify(self.getters))

    def __add__(self, other):
        blocks = self.block + other.block
        getters = listify(self.getter) + listify(other.getter)
        kwargs = merge_kwargs(self.tmms, other.tfms)

        return DataBox(blocks, getters, **kwargs)
