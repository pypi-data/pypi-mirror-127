#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

extras_require = \
{'test': ['pytest >=2.7.3']}

setup(name='unique_defaults',
      version='0.1',
      description="Replace a function's default values with objects unique to each function call",
      author=None,
      author_email='Jeremiah Paige <ucodery@gmail.com>',
      url=None,
      py_modules=['unique_defaults'],
      extras_require=extras_require,
     )
